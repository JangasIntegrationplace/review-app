name: CD
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      uses: appleboy/ssh-action@master
      with:
        password: ${{ secrets.secrets.PASSWORD }}
        username: ${{ secrets.secrets.USERNAME }}
        host: ${{ secrets.secrets.HOST }}
        script: |
          whoami
          ls -al
