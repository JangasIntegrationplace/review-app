name: CD
on:
  push:
    branches:
      - master

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      uses: appleboy/ssh-action@master
      with:
        password: ${{ env.PASSWORD }}
        username: ${{ env.USERNAME }}
        host: ${{ env.HOST }}
        script: |
          whoami
          ls -al
